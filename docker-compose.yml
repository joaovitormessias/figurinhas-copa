services:
  postgres:
    image: postgres:16
    container_name: figurinhas-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: figurinhas_dev
    ports:
      - "5433:5432"
    volumes:
      - figurinhas_postgres_data:/var/lib/postgresql/data

volumes:
  figurinhas_postgres_data:
